apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-access-token-server.fullname" . }}
  labels:
    {{- include "github-access-token-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "github-access-token-server.selectorLabels" . | nindent 4 }}
