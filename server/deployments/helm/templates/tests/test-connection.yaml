apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "github-access-token-server.fullname" . }}-test-connection"
  labels:
    {{- include "github-access-token-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "github-access-token-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
